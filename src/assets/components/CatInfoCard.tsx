import { Cat } from ".../Cat.ts";
import React from "react";

interface CatInfoCardProps {
  cat: Cat;
  isCorrect: boolean;
}

// const CatInfoCard: React.FC<CatInfoCardProps> = ({ cat, isCorrect }) => {

// };
// export default CatInfoCard;
